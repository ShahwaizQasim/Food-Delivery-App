import { NextPage } from "next";

interface Props {}

const DashboardContent: NextPage<Props> = ({}) => {
  return (
    <div>
      <h1>Dashboard</h1>
    </div>
  );
};

export default DashboardContent;
